# Optional endpoint to capture leads before redirecting to WhatsApp.
# Set this only if you have a backend service for lead capture.
VITE_LEAD_ENDPOINT=""

# WhatsApp number in international format without the + prefix.
# The default number is the AUREL sales contact.
VITE_WHATSAPP_NUMBER="923323632052"
